// test("Authorization by Internet login сheck for XSS security issues", async () => {
//     await loginPage.login(globalData.xssInjection, globalData.defaultPassword);
//     await expect(loginPage.usernameInputHelper).toContainText(errorMessages.ua.invalidValue);
//   });
